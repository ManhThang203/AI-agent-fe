/** Ô nhập + gửi — implement ở task auth-api / chat. */
export function Composer() {
  return (
    <div className="mt-auto border-t border-zinc-800 pt-4">
      <div className="rounded-lg border border-zinc-700 bg-zinc-900/50 px-3 py-2 text-sm text-zinc-500">
        Composer (sắp có)
      </div>
    </div>
  );
}
