import { useEffect } from "react";

/**
 * Subscribe kênh agent-user-{userId} — logic đầy đủ ở task pusher-hook.
 * @param {object} _params
 * @param {string|undefined} _params.userId
 */
export function useAgentPusher(_params) {
  useEffect(() => {}, []);
}
